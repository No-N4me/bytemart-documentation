# Upper bounds are deliberate. MkDocs 2.0 removes the plugin system and rewrites
# theming, which would break the i18n plugin, the build hook, and the templates in
# overrides/ — see https://squidfunk.github.io/mkdocs-material/blog/2026/02/18/mkdocs-2.0/
mkdocs>=1.6,<2
mkdocs-material>=9.7.6,<10   # overrides/partials/header.html tracks the 9.7.x header
mkdocs-static-i18n>=1.3.1,<2
jieba>=0.42.1,<1             # Chinese word segmentation for accurate search
